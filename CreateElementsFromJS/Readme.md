use create Element to create any tags(h1 to h6, p,div,)--head= document.createElement("taganme")
assign the value to tags -->head.innerText="some";
append it to body=document.querySelector("body").append(head);

--------Attribute set
